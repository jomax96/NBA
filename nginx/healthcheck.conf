# Health check configuration para NGINX
# Este archivo se incluye automáticamente

upstream backend_health {
    server backend-node1:3000;
    server backend-node2:3000;
    server backend-node3:3000;
}

